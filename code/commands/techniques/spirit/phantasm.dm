Command/Technique/Form
	phantasm
		name = "phantasm"
		internal_name = "phantasm"
		format = "~phantasm";
		priority = 1
		iCommand = FALSE;
		tType = UTILITY;
		formName = "Phantasm"
		helpDescription = "Starting form of a newly create Spirit. Still innocent and fresh from death."

		getSyntax() {
			return NULL
		}

		command(mob/user) {
			return
		}
