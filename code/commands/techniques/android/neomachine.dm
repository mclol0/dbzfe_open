Command/Technique/Form
	neomachine
		name = "neomachine"
		internal_name = "neomachine"
		format = "neomachine";
		priority = 1
		iCommand = FALSE;
		tType = UTILITY;
		formName = "NeoMachine"
		helpDescription = "Neo Machine is the remorted starting transformation of Androids."

		getSyntax() {
			return NULL
		}

		command(mob/user) {
			return
		}
